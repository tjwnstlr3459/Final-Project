<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="category">
	<select id="selectAllCategory" parameterType="_integer" resultType="cg">
		select cg_no as cgNo, cg_name as cgName, cg_kinds as cgKinds from category
		
	</select>
	<select id="selectCategory" resultType="cg">
		select cg_no as cgNo, cg_name as cgName, cg_kinds as cgKinds from category where cg_kinds=1		
	</select>
</mapper>
